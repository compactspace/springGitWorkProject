<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ReservePaymentMapper">

	<select id="dupulicateCheckForReservePayment"
		parameterType="String" resultType="Map">
		select * from reserve_payment rp
		where
		merchant_uid=#{merchantUid};
	</select>

	<select id="checkClientPriceEqualsToRecentUpdatedPrice"
		parameterType="String" resultType="Integer">

		SELECT
		oh.onedayclass_price
		FROM
		draft_reserveinfo dr
		LEFT JOIN onedayclass_history oh
		ON oh.onedayclass_num = dr.onedayclass_num
		WHERE
		dr.merchant_uid = #{merchantUid};
	</select>

	<update id="updateDraftReserveStatusTo" parameterType="String">
		update draft_reserveinfo set status='PAID'
		where merchant_uid=#{merchantUid}
	</update>


</mapper>