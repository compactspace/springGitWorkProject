<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="PaymentDAO">

<insert id="insertPayment" parameterType="PaymentDTO" useGeneratedKeys="true" keyProperty="paymentId">
    INSERT INTO payment (order_info_id, payment_number, amount, payment_method, created_at)
    VALUES (#{orderInfoId}, #{paymentNumber}, #{amount}, #{paymentMethod}, NOW())
</insert>

</mapper>
