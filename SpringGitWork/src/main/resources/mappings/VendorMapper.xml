<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="VendorMapper">

	<!-- 신규 거래처 등록 -->
	<insert id="newVendorInsert"
		parameterType="newInsertVendorRequestDTO" useGeneratedKeys="true"
		keyProperty="vendorId">
		INSERT INTO vendor
		(name, business_number, settlement_type, delivery_type, status)
		VALUES
		(#{name}, #{businessNumber}, #{settlementType}, #{deliveryType}, #{status})
	</insert>

	<select id="getVendorList" resultType="GetVendorListDTO">
		select vendor_id AS vendorId, name AS name from vendor
	</select>


</mapper>
